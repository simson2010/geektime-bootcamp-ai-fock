---
import PageLayout from '../../layouts/PageLayout.astro';
import PageHeader from '../../components/ui/PageHeader';
import ProjectCard from '../../components/projects/ProjectCard';
import ScrollReveal from '../../components/ui/ScrollReveal';
import { projects } from '../../data/projects';
import { getUrl } from '../../utils/url';
---

<PageLayout
  title="实战项目 - 陈天 AI 训练营"
  description="通过 5 个实战项目，从零到一掌握 AI 辅助编程"
>
  <PageHeader
    title="实战项目"
    subtitle="从零到一，用 AI 构建真实项目"
    client:load
  >
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a href="#projects" class="btn-primary"> 查看项目 </a>
      <a href={getUrl('curriculum')} class="btn-secondary"> 返回课程 </a>
    </div>
  </PageHeader>

  <section class="py-24 bg-bg-primary">
    <div class="container-custom">
      <ScrollReveal client:load>
        <h2 class="text-3xl md:text-4xl font-bold text-center text-primary mb-4">
          循序渐进的项目设计
        </h2>
        <p class="text-center text-text-secondary mb-16 max-w-2xl mx-auto">
          从简单到复杂，每个项目都对应特定的学习目标，逐步提升你的 AI 辅助编程能力
        </p>
      </ScrollReveal>

      <div class="grid grid-cols-1 gap-8 mb-12">
        {
          projects.map((project, index) => (
            <ScrollReveal delay={index * 0.1} client:load>
              <div class="flex flex-col md:flex-row gap-8 items-start">
                <div class="md:w-2/3">
                  <ProjectCard
                    number={project.number}
                    title={project.title}
                    subtitle={project.subtitle}
                    difficulty={project.difficulty}
                    estimatedHours={project.estimatedHours}
                    techStack={project.techStack}
                    href={getUrl(`projects/project-${project.number}`)}
                    previewImage={project.previewImage}
                    client:load
                  />
                </div>
                <div class="md:w-1/3 space-y-4">
                  <div class="p-6 bg-bg-secondary rounded-xl">
                    <h4 class="font-semibold text-primary mb-3">学习目标</h4>
                    <ul class="space-y-2 text-sm text-text-secondary">
                      {project.objectives.slice(0, 3).map((obj) => (
                        <li class="flex gap-2">
                          <span class="text-accent">•</span>
                          <span>{obj}</span>
                        </li>
                      ))}
                    </ul>
                  </div>
                  <div class="p-4 bg-accent/10 rounded-xl text-center">
                    <div class="text-sm text-text-secondary mb-1">所属周次</div>
                    <div class="text-2xl font-bold text-accent">
                      第 {project.weekNumber} 周
                    </div>
                  </div>
                </div>
              </div>
            </ScrollReveal>
          ))
        }
      </div>
    </div>
  </section>

  <section class="py-24 bg-bg-secondary">
    <div class="container-custom">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
        <div class="text-center p-8 bg-white rounded-xl">
          <div class="text-4xl font-bold text-accent mb-2">5</div>
          <div class="text-text-secondary">个实战项目</div>
        </div>
        <div class="text-center p-8 bg-white rounded-xl">
          <div class="text-4xl font-bold text-accent mb-2">44+</div>
          <div class="text-text-secondary">小时实践</div>
        </div>
        <div class="text-center p-8 bg-white rounded-xl">
          <div class="text-4xl font-bold text-accent mb-2">10+</div>
          <div class="text-text-secondary">项技术栈</div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-24 bg-bg-primary">
    <div class="container-custom text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-primary mb-8">
        准备好动手实践了吗？
      </h2>
      <p class="text-xl text-text-secondary mb-8 max-w-2xl mx-auto">
        从第一个项目开始，体验 AI 辅助编程的强大力量
      </p>
      <a href={getUrl('projects/project-1')} class="btn-primary"> 开始第一个项目 </a>
    </div>
  </section>
</PageLayout>
